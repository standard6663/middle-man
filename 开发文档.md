# 开发文档

## 端口8443数据存储修改

### 需求

当目标端口为 8443 时，写入数据库的 `protocol_version` 替换为 `SSL3.0`，`cipher_suite` 替换为 `SSL_RSA_WITH_RC4_128_MD5`。

### 实施方案

**修改文件**：`midman/reporter.py`

**修改位置**：第391-404行，`_db_writer_worker` 方法中 `insert_packet_session` 调用前

**修改条件**：目标端口 `destination_port == 8443`

**替换值**：
- `protocol_version` → `SSL3.0`
- `cipher_suite` → `SSL_RSA_WITH_RC4_128_MD5`

### 新增文件

- `delay_field_explanation.md`：delay 字段计算说明文档

### 实施日期

2026-05-20